<div class="form" style="opacity: 0;">
    <%= form_for(:user, :url => registration_path(:user)) do |f| %>
    <p>
        Nome:<br>
        <%= f.text_field :name, autofocus: true, disabled: true %>
    </p>
    <p>
        E-mail:<br>
        <%= f.email_field :email, autofocus: true, disabled: true %>
    </p>
    <p> 
        Senha:<br>
        <%= f.password_field :password, autocomplete: "off", disabled: true %>
    </p>
    <p>
        Confirmar senha:<br>
        <%= f.password_field :password_confirmation, autocomplete: "off", disabled: true %>
    </p>
        <%= f.submit "Cadastrar", class: "mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent", disabled: true %>
    <% end %>
</div>